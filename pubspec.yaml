name: todo
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.17.3 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  supabase_flutter: ^0.3.1+3
  shared_preferences: ^2.0.15
  cool_alert: ^1.1.0
  drift: ^1.7.0 #Persistant Database locally
  provider: ^6.0.3
  auto_size_text: ^3.0.0 #Resize Text
  equatable: ^2.0.3
  rename: ^2.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
